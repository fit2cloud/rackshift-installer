# RackShift 安装管理包

installer 可以安装、部署、更新 管理 RackShift

## 安装部署

```bash
cd rackshift-installer/installer
./install.sh
```

## 管理

```bash
# 启动
rsctl start

# 停止
rsctl stop

# 升级
rsctl uninstall

# 卸载
rsctl uninstall

# 帮助
rsctl --help
```
